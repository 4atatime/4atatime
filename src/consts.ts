// Global strings. Kept apart from each other on purpose: the tab title, the
// sentence shared to social cards, and the hello in the corner of the page all
// used to be the same two values, which meant the greeting couldn't change
// without rewriting what search engines quote.

/** Tab title, and what a shared link is called. */
export const SITE_TITLE = '4atatime';

/** The blurb under a shared link, and the page's meta description. */
export const SITE_DESCRIPTION = ': a reasonable amount of trouble :';

/** The hello in the top-left corner. Not metadata; just the welcome. */
export const SITE_GREETING = "Halöòóõ! You're now at Lexie's web garden <3";
